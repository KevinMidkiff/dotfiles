Random Notes
============

This contains random system configuration notes.

## Gnome Solid Color Background

To set solid color background in gnome, these commands are helpful:

```sh
gsettings set org.gnome.desktop.background picture-uri ""
gsettings set org.gnome.desktop.background primary-color '#192e21'
```
